<form method="post" action="{{ path('reservation_excursion_delete', {'id': reservation_excursion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservation_excursion.id) }}">
    <button class="btn">Delete</button>
</form>
