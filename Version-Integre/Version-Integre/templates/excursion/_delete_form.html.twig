<form method="post" action="{{ path('excursion_delete', {'id': excursion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ excursion.id) }}">
    <button class="btn">Delete</button>
</form>
