<form method="post" action="{{ path('reservation_voyage_delete', {'id': reservation_voyage.id}) }}" onsubmit="return confirm('Are you sure you want to delete this Reservation?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservation_voyage.id) }}">
    <button class="btn">Delete</button>
</form>
